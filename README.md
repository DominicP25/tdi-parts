# TDI Parts Tracker (public tag pages)

Static page technicians reach by scanning a part's QR code. Each code opens that item's request page via a URL fragment (e.g. `#tag=TDI-TL-002`).

Admin tracker is NOT here — it lives in the localhost app and the private Claude artifact.
